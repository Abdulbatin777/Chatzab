services:
  postgres:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_DB: chatzab
      POSTGRES_USER: chatzab
      POSTGRES_PASSWORD: chatzab
    ports: ["5432:5432"]
    volumes: ["chatzab_pg:/var/lib/postgresql"]
volumes: {chatzab_pg: {}}
